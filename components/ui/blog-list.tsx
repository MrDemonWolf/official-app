// Deprecated placeholder — listing is now handled in `app/blog.tsx` using `wp-client`.
import React from 'react'
import { View, Text } from 'react-native'

export default function DeprecatedBlogList() {
  return (
    <View className="p-4">
      <Text className="text-gray-600">Deprecated: Use `app/blog.tsx` with the WP client</Text>
    </View>
  )
}
